Проверить после сборки, что зависимостей нет:
```
REM если есть dumpbin (идёт с VS):
dumpbin /dependents agent.exe
REM должно остаться только KERNEL32.dll, WS2_32.dll, возможно msvcrt.dll
```


Копируете agent.exe + ffmpeg.exe на «чистую» машину — запускается без установки чего-либо.